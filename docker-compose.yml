# ==============================================================================
# Tabrela - Docker Compose Configuration
# ==============================================================================
# This file orchestrates the entire Tabrela backend stack.
# 
# Usage:
#   Production: docker-compose up -d
#   Development: docker-compose -f docker-compose.yml -f docker-compose.dev.yml up
# ==============================================================================

version: '3.8'

services:
  # ============================================================================
  # Backend Services (all-in-one container)
  # ============================================================================
  backend:
    image: ${DOCKER_REGISTRY:-ghcr.io}/hamza-bin-aamir/tabrela-backend:${IMAGE_TAG:-latest}
    container_name: tabrela-backend
    restart: unless-stopped
    ports:
      - "8081:8081"  # Auth service
      - "8082:8082"  # Attendance service
      - "8083:8083"  # Merit service
      - "5000:5000"  # Email service
    environment:
      # Database
      - DATABASE_URL=${DATABASE_URL}
      
      # Auth Service Config
      - AUTH_JWT_SECRET=${AUTH_JWT_SECRET}
      - AUTH_JWT_EXPIRY=${AUTH_JWT_EXPIRY:-3600}
      - AUTH_REFRESH_EXPIRY=${AUTH_REFRESH_EXPIRY:-604800}
      
      # Email Service Config
      - RESEND_API_KEY=${RESEND_API_KEY}
      - EMAIL_FROM=${EMAIL_FROM:-noreply@tabrela.com}
      
      # Service URLs (internal)
      - AUTH_SERVICE_URL=http://localhost:8081
      - ATTENDANCE_SERVICE_URL=http://localhost:8082
      - MERIT_SERVICE_URL=http://localhost:8083
      - EMAIL_SERVICE_URL=http://localhost:5000
      
      # General
      - RUST_LOG=${RUST_LOG:-info}
      - ENVIRONMENT=${ENVIRONMENT:-production}
    volumes:
      - tabrela-logs:/var/log/tabrela
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8081/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s
    networks:
      - tabrela-network
    depends_on:
      db:
        condition: service_healthy

  # ============================================================================
  # PostgreSQL Database
  # ============================================================================
  db:
    image: postgres:16-alpine
    container_name: tabrela-db
    restart: unless-stopped
    environment:
      - POSTGRES_USER=${POSTGRES_USER:-tabrela}
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD}
      - POSTGRES_DB=${POSTGRES_DB:-tabrela}
    volumes:
      - tabrela-db-data:/var/lib/postgresql/data
      - ./services/migrations:/docker-entrypoint-initdb.d:ro
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-tabrela} -d ${POSTGRES_DB:-tabrela}"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - tabrela-network

# ==============================================================================
# Networks
# ==============================================================================
networks:
  tabrela-network:
    driver: bridge

# ==============================================================================
# Volumes
# ==============================================================================
volumes:
  tabrela-db-data:
    driver: local
  tabrela-logs:
    driver: local
