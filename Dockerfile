# ==============================================================================
# Tabrela - Docker Configuration
# ==============================================================================
# The main backend Dockerfile is located at: docker/Dockerfile.backend
# 
# To build and run:
#   docker compose up -d
#
# For development:
#   docker compose -f docker-compose.yml -f docker-compose.dev.yml up
#
# See docs/VPS_DEPLOYMENT.md for full deployment instructions
# ==============================================================================

# This is a placeholder. Use docker/Dockerfile.backend for the actual build.
FROM scratch
LABEL maintainer="Hamza Bin Aamir"
LABEL description="Tabrela - see docker/Dockerfile.backend for actual build"